# Valid Account instance using schema-defined slots
# Account is_a Agreement which defines name and description properties

name: "Primary Checking Account"
description: "A bank account with transaction history and financial balance tracking"
